apiVersion: v1
kind: Service
metadata:
  name: {{ include "ice-storageclass.fullname" . }}
  labels:
    {{- include "ice-storageclass.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ice-storageclass.selectorLabels" . | nindent 4 }}
