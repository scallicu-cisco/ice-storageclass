apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ice-storageclass.fullname" . }}-test-connection"
  labels:
    {{- include "ice-storageclass.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ice-storageclass.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
